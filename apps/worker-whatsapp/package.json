{
  "name": "@whatsapp-platform/worker-whatsapp",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test:integration:outbound-worker": "vitest run --config vitest.outbound-worker.config.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@whatsapp-platform/config": "workspace:*",
    "@whatsapp-platform/database": "workspace:*",
    "@whatsapp-platform/messaging": "workspace:*"
  }
}
