{
  "name": "@whatsapp-platform/database",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./platform": {
      "types": "./src/platform.ts",
      "require": "./dist/platform.js",
      "default": "./dist/platform.js"
    }
  },
  "scripts": {
    "build": "pnpm db:generate && tsc -p tsconfig.json",
    "db:generate": "prisma generate --config prisma.config.ts",
    "db:migrate:deploy": "prisma migrate deploy --config prisma.config.ts",
    "db:migrate:dev": "prisma migrate dev --config prisma.config.ts",
    "db:studio": "prisma studio --config prisma.config.ts",
    "db:validate": "prisma validate --config prisma.config.ts",
    "rbac:sync-permissions": "pnpm --filter @whatsapp-platform/rbac build && pnpm build && node dist/rbac-sync.js",
    "test:integration": "pnpm --filter @whatsapp-platform/config build && pnpm --filter @whatsapp-platform/rbac build && vitest run --config vitest.integration.config.mts",
    "test:integration:rbac": "pnpm --filter @whatsapp-platform/config build && pnpm --filter @whatsapp-platform/rbac build && vitest run --config vitest.rbac.config.mts",
    "test:integration:platform-tenants": "pnpm --filter @whatsapp-platform/config build && vitest run --config vitest.platform-tenants.config.mts",
    "test:integration:tenant-provisioning": "pnpm --filter @whatsapp-platform/config build && vitest run --config vitest.tenant-provisioning.config.mts",
    "test:security:tenant-isolation": "pnpm --filter @whatsapp-platform/config build && pnpm --filter @whatsapp-platform/rbac build && vitest run --config vitest.tenant-isolation.config.mts",
    "typecheck": "pnpm db:generate && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prisma/adapter-pg": "7.9.1",
    "@prisma/client": "7.9.1",
    "@whatsapp-platform/config": "workspace:*",
    "@whatsapp-platform/rbac": "workspace:*"
  },
  "devDependencies": {
    "prisma": "7.9.1"
  }
}
